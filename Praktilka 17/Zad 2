def is_prime(num):
    prime = True
    if num > 1:
        for i in range(2, int(num**0.5) + 1):
            if num % i == 0:
                prime = False
                break
    return prime

print(is_prime(input("Введите число ")))
